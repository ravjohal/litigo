<% if activity.trackable %>
  updated expense <%= link_to "View", expense_path(activity.trackable.id ), :class => 'view_activity' %>
<% else %>
  updated expense which has since been removed
<% end %>