<% if activity.trackable %>
    created document <%= link_to "View", document_path(activity.trackable.id ), :class => 'view_activity', :class => 'view_activity'%>
<% else %>
    added a document which has since been removed
<% end %>