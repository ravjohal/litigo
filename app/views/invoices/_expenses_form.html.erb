<div class="modal-dialog">
  <div class="modal-content">
    <div class="modal-header">
      <a class="close" style="padding-top: 15px;" data-dismiss="modal">×</a>

      <h1>Add Expenses to Invoice #<%= params[:number] %></h1>
    </div>
    <div class="modal-body">

      <table class="lead-form table-condensed">
        <thead>
        <tr>
          <th></th>
          <th>Staff Member</th>
          <th>Expense</th>
          <th>ABA Code</th>
          <th>Amount</th>
        </tr>
        </thead>
        <tbody>
        <% @expenses = @case.expenses %>
        <%= render 'invoices/expenses_table' %>
        </tbody>
      </table>
      <br>

    </div>

    <div class="modal-footer">
      <div class="pull-left">
        <a class="close" data-dismiss="modal">Cancel</a>
      </div>

      <div class="form-actions">
        <%= button_tag 'Add Expenses', type: :submit, :class => 'btn btn-info', :style => 'font-size: 18px;', id: 'confirm_add_expenses' %>
      </div>

    </div>

  </div>

</div>